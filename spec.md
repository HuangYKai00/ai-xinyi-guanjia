# AI 心意管家 — 产品规格文档（v1.1）

## 一、联系人管理

### 1. 添加联系人
【用户可以】将自己在乎的人录入 App，包括家人、挚友、重要客户等。
- 必填项：姓名、关系、生日（生日为必填，纪念日为选填，但至少要有生日）；
- 选填项：特征描述（允许不填，保存时非强制）；
- "关系"为自由文本，由用户自行输入（如"大学室友老王""客户"）；系统会将常见同义表达归一化到标准关系，用户界面仍展示其原始输入，归一化结果仅用于身份自动关联、关系反推落款等内部逻辑（对照表见下方）；
- 一个联系人可添加多个日子（一对多，无数量上限）；
- 每个纪念日用户可自定义命名（如"结婚纪念日""入职纪念日"）；
- 生日可填写出生年份，但非强制。

**关系同义归一化对照表（v1）：**

| 标准关系 | 常见同义表达 |
|---|---|
| 母亲 | 妈妈、母亲、妈咪、老妈、娘、娘亲 |
| 父亲 | 爸爸、父亲、爸、老爸、爹、爹爹 |
| 妻子 | 老婆、妻子、太太、夫人、内人 |
| 丈夫 | 老公、丈夫、先生、夫君 |
| 儿子 | 儿子、小子、男娃 |
| 女儿 | 女儿、闺女、丫头、女娃 |
| 公公 | 公公、家公 |
| 婆婆 | 婆婆、家婆、婆母 |
| 岳父 | 岳父、老丈人、丈人、岳丈 |
| 岳母 | 岳母、丈母娘、岳母娘 |

存在歧义的表达（如"爱人"可指丈夫或妻子、"媳妇"在部分地区指儿媳）不纳入自动归一化，由用户手动明确。

**验收标准：** 成功录入后，联系人的姓名、关系、日子、特征描述等全部保存并可随时查看；必填项缺失时无法保存；同一个联系人名下可以存在并展示多个日子。

**为什么：** 联系人是所有后续心意推送的基础数据；一对多的日子模型符合真实生活——一个人往往有多个值得纪念的日子。

### 2. 日子的来源（手动设定 + 身份自动关联）
【用户可以】通过两种方式获得日子：① 手动为联系人设定纪念日；② 由联系人身份自动关联——例如"妻子"自动关联妇女节，"母亲"自动关联妇女节和母亲节。身份识别基于归一化后的标准关系（见功能 1），系统识别到对应关系时自动为联系人补上相关节日。

**验收标准：** 手动设定的纪念日与身份自动关联的日子在联系人详情中都能被看到和区分；自动关联的日子随关系字段变化而增减；自动关联的日子用户可单独编辑或改名（与手动日子同等待遇）；儿子/女儿因无法判断年龄，不自动关联儿童节，用户如需可为子女手动添加儿童节。

**关系 → 自动关联节日映射表：**

| 标准关系 | 自动关联节日 |
|---|---|
| 母亲 | 妇女节、母亲节 |
| 父亲 | 父亲节 |
| 妻子 | 妇女节 |
| 丈夫 | 无 |
| 儿子 | 无（不自动关联儿童节，如需可手动添加） |
| 女儿 | 无（同上） |
| 公公 | 父亲节 |
| 婆婆 | 妇女节、母亲节 |
| 岳父 | 父亲节 |
| 岳母 | 妇女节、母亲节 |

**为什么：** 用户往往只记得填生日，漏掉与身份相关的节日。自动关联能兜住这些高价值的提醒场景。

### 3. 编辑联系人信息
【用户可以】随时修改已录入联系人的任意信息（姓名、关系、日期、特征描述等）。

**验收标准：** 修改后立即生效，后续的祝福生成和礼物推荐均基于最新信息；关系字段变化会触发身份自动关联日子的重新计算——对于因关系变更而新增的自动关联日子，若用户之前手动删除过同类型的日子，弹窗询问用户是否重新添加（而非静默添加）。

**为什么：** 人的状态会变化（退休、换工作、新爱好），信息保持准确才能生成贴切的心意。

### 4. 删除联系人
【用户可以】从列表中移除不再需要关心的联系人。

**验收标准：** 删除后，与该联系人相关的所有提醒不再触发；已生成的祝福、礼物、贺卡等历史内容保留在本地，仍可在历史记录中查看；已生成但尚未写入历史记录（日子未到）的祝福在删除时随联系人一并丢失，不做保留。

**为什么：** 人际关系会变化，用户应有完全的控制权；同时历史内容保留，避免误删导致过去的心意记录一并消失。

### 5. 已过日子的处理
【用户可以】看到已经过去的日子。按生效方式不同处理："每年一次"的日子继续保留在生效中的提醒列表，自动进入下一年度提醒；"仅提醒一次"的日子在生效过后，于次日从"生效中"移到提醒列表最下方的"已过期"分区，与生效中的提醒分区展示（层级分开），不再触发提醒。已过的日子会同步保存进历史记录。

**验收标准：** "每年一次"的日子到期后仍在生效中的提醒列表，自动进入下一年度；"仅一次"的日子到期后移至提醒列表最下方的过期分区，提醒开关永久关闭，与生效中的提醒在视觉上分区区分；已过日子的记录保存在历史记录中，历史记录里的条目只能在该页面手动删除，不会因提醒列表中的删除而被连带删除。

**为什么：** 保留记录是必要的，但过期日子若继续提醒会变成骚扰；同一列表内"生效中 / 已过期"分区展示，让用户在一个页面就能看到全貌，又不会混淆。

---

## 二、智能提醒

### 6. 特殊日子自动提醒
【用户可以】收到 App 在联系人生日或纪念日临近时的主动推送。
- 提醒节奏：按距离日子的剩余天数计算——剩余 ≥7 天时每周提醒一次，触发日为与日子当天相同的星期几（如日子是周三，则每周三提醒，第 7 天为每周提醒的最后一次）；剩余 4~6 天时不提醒；剩余 ≤3 天时每天提醒一次（第 3、2、1 天及当天均提醒）；
- 周期规则：生日每年一次；纪念日默认仅生效一次，若需每年重复提醒由用户手动开启"每年一次"；"每年一次"的纪念日到期后自动进入下一年度提醒，"仅一次"的到期后提醒开关永久关闭；
- 局部关闭（两个粒度）：用户可手动关闭单个日子的提醒，也可关闭整个联系人的提醒；
- 同一天有多个提醒时，合并规则：仅同一节日触发的"纯节日提醒"与"关联联系人提醒"（如母亲节对用户本人 + 母亲节关联母亲）会合并为一条通知；不同日子（如某人结婚纪念日与另一人的生日）或不同节日之间不会合并；
- 2 月 29 日生日：非闰年时于 2 月 28 日按生日提醒（提前一天）。

**验收标准：** 通知内容须包含联系人姓名、日子类型（生日/纪念日）以及剩余天数；节奏按"≥7 天每周一次、4~6 天静默、≤3 天每天一次"执行；同一节日的纯节日提醒与关联联系人提醒合并为一条通知，点开进入变体 A 详情页；不同日子之间不合并，各自独立展示。

**为什么：** 用户可能忙碌或遗忘，主动提醒是"AI 主动关怀"的核心价值；由疏到密的提醒节奏，在不过度打扰的前提下保证用户不会错过。

### 7. 提醒提前天数可调整
【用户可以】自行设置希望提前多少天收到提醒，可调范围为 1–60 天；提前天数决定提醒窗口的起点，窗口内按功能 6 的节奏提醒（如设为 30 天，则从距日子 30 天起进入每周提醒，最后 3 天转为每天提醒）；若设置的提前天数小于 7 天（即 1~6 天），则从窗口第一天起每天提醒，直到日子当天；即使只设置提前 1 天，日子当天仍会提醒一次。提醒规则的具体说明在设置页提供入口查看。

**验收标准：** 用户修改后，该规则对个人日子与通用节日共同生效（统一一套设置），下一次提醒按新设置触发；超出 1–60 范围的值不允许保存。

**为什么：** 不同用户的准备习惯不同——有人提前一周准备礼物，有人前一天才想起来；统一一套设置降低理解成本。

### 8. 通用节日自动提醒
【用户可以】在通用节日临近时收到提醒。
- 提醒对象默认是"用户自己"，不绑定具体联系人；
- 仅当节日明确指向特定人群、且用户已设置了相关关系时，才自动关联对应联系人（例如母亲节自动关联"关系=母亲"的联系人）；关联联系人后，该节日详情页与个人日子提醒一致，为该联系人生成祝福、礼物、贺卡；同一节日触发的"纯节日提醒"与"关联联系人提醒"若落在同一天，按功能 6 的合并规则合并为一条通知——且合并后点开详情页显示的均为变体 A（关联联系人的个性化祝福+礼物+贺卡）；不同节日或节日与个人日子之间不合并；
- 未关联到联系人的节日（用户未添加任何联系人，或节日本身不指向特定人群）仍照常推送提醒；点开详情页后，用户需点击一次"生成"按钮，才会生成一张给用户本人的节日祝福贺卡，文案无需用户选择；
- 未关联联系人的节日与联系人日子混排在同一份提醒列表中（默认按剩余天数升序），同时支持切换为按联系人排序的查看方式；
- 通用节日提醒默认开启，可在设置中控制——节日提醒拆为 3 个开关，层级关系如下：① 为总开关，关闭后所有节日提醒（含已关联联系人和未关联联系人的节日）均不推送；② 和 ③ 为子开关，仅在 ① 开启时才生效——② 控制未关联联系人的节日提醒、③ 控制已关联联系人的节日提醒；当 ① 处于关闭状态时，② 和 ③ 变为不可操作的灰色状态；① 开启后，② 和 ③ 恢复正常可操作；默认 3 个开关均开启；
- 节日日期以中国通用标准为准，中国没有的节日以国际通用标准为准；节日日期按以下方式确定：① 固定公历节日（元旦 1/1、妇女节 3/8、劳动节 5/1、儿童节 6/1、国庆节 10/1、圣诞节 12/25、情人节 2/14）按固定月日；② 相对日期节日（母亲节 = 5 月第 2 个周日、父亲节 = 6 月第 3 个周日）按规则推算；③ 农历节日（春节、元宵节、除夕、端午节、七夕节、中秋节、重阳节）与清明节通过读取手机系统日历获取当年准确日期；

**v1 支持节日清单：**

中国法定节假日：元旦、春节、清明节、劳动节、端午节、中秋节、国庆节

传统节日：元宵节、七夕节、重阳节、除夕

特定人群/主题节日：母亲节、父亲节、妇女节、儿童节

西方/商业节日：圣诞节、情人节

**验收标准：** 每个已支持的节日到来前，App 按提前天数设置发出推送，通知包含节日名称与剩余天数；关联规则正确触发（母亲节→母亲）；清单之外的节日不推送。

**为什么：** 通用节日是表达心意的高频场景；"提醒自己 + 条件关联联系人"的模型，既能覆盖纯节日提醒，也能在关系匹配时升级为个性化建议。

### 9. 推送通知开关
【用户可以】选择开启或关闭整个 App 的推送通知功能。

**验收标准：**
- 关闭后，所有提醒（个人日子 + 通用节日）均不再推送，重新开启后恢复；
- 关闭期间错过的提醒不补发，但恢复后弹窗以摘要方式告知（如"您错过了 X 个提醒，点击查看详情"），点击后进入提醒 Tab 可查看错过的提醒明细；
- 系统通知权限被关闭时，恢复权限后显示"错过提醒"弹窗。

**为什么：** 强制推送会造成反感甚至卸载；权限被系统关闭是常见情况，主动告知错过的日子体现"体贴"，避免用户不知情地错过重要日子。

---

## 三、祝福内容生成

### 10. 个性化祝福生成
【用户可以】从推送点进详情页时，实时看到已经拟好、结合被提醒对象特征量身定制的祝福语。
- 祝福语在用户点进详情页时实时生成；生成后缓存并持久化到本地磁盘，用户退出再进详情页、或杀进程后重新打开 App，仍显示同一批，直到主动点击"重新生成"才更新；同一联系人在同一提醒年度内的多次提醒（如提前 30 天、提前 7 天、前 3 天）共用同一个详情页 session——只显示最新一次提醒日期，祝福只保留用户最后一次修改生成的那批；下一年度的提醒（"每年一次"的日子自动进入下一年）为新开 session，上一年度的缓存不跨年度保留；
- 生成依据：关系类型、特征描述、日子类型（生日/纪念日/节日）；
- 无特征描述时，按关系类型生成，并引导（非强制）用户补充特征描述；
- 无网络时提示用户连接网络；
- 每条祝福语长度不超过 600 字，纯文本格式，适合直接复制到聊天软件发送。

**验收标准：** 祝福语与被提醒对象直接相关（如结合"爱养花，今年刚退休"），而非泛泛通用祝福；点进详情页即能呈现；断网时给出明确的联网提示而非空白或错误。

**为什么：** 用户最头疼的不是"要不要送祝福"，而是"不知道该说什么"；直接给出结合对方特点的祝福语，用户只需挑选或微调即可发送。

### 11. 祝福重新生成
【用户可以】对生成的祝福不满意时，一键重新生成全部祝福语。
- 默认每次生成 3 条，不支持调整条数；
- 重新生成为全量重新生成，不支持单条单独重新生成。

- 重新生成不限次数，用户可反复点击直到满意；
- 生成失败（网络异常、服务超时等）时，保留原有内容不变，并给出失败提示与重试入口。

**验收标准：** 每次点"重新生成"后，3 条祝福语整体被替换为新的一批；条数始终为 3；反复点击不受次数限制；生成失败时旧内容不丢失，用户可重试。

**为什么：** 固定条数保持界面简洁、成本可控；"全部重来"比逐条重试更符合用户"换一批"的心智模型。

### 12. 祝福语交互
【用户可以】点击某条祝福语将其选定（用于贺卡文案），选中状态与未选中要有明显的视觉区分；每条祝福语旁附带一个独立的复制 icon，点击后将该条祝福语复制到剪贴板。"选中"与"复制"是两个独立操作，互不干扰。首次进入详情页时，3 条祝福语均为未选中状态，等待用户手动点击选择；一旦用户发生了选中行为，则不支持取消选中回到全无选中状态——用户可通过点击另一条祝福语切换选择，但始终至少有一条处于选中态。

**验收标准：** 点击祝福语文本 = 选中（此条高亮，用于贺卡）；点击复制 icon = 复制到剪贴板；首次进入时祝福均不选中，用户手动点击后进入"始终至少一条选中"态；选中态与未选中态视觉差异清晰可辨；选中后无法反选回到全空，只能切换到另一条。

**为什么：** "复制发出去"和"选为贺卡文案"是两个独立意图，合二为一会造成用户误操作，分开更符合预期。

---

## 四、礼物推荐

### 13. 个性化礼物建议
【用户可以】在提醒详情页看到多条与被提醒对象匹配的礼物建议。
- 生成时机：与祝福语一致，点进详情页时实时生成；
- 默认每次推荐 5 个；
- 生成依据：特征描述、日子类型、关系类型；
- 支持用户输入"礼物方向"（如"实用""浪漫""预算500以内"）后重新生成；
- 重新生成为全量重新生成，不支持单条单独重新生成。

- 重新生成不限次数；生成失败时保留原有内容不变，并给出失败提示与重试入口；
- 无网络时，礼物区给出明确的联网提示与重试入口，而非空白或报错。

**验收标准：** 礼物建议结合对方特点（如"爱养花"建议园艺工具、"刚退休"建议旅行用品），而非随机推荐；输入礼物方向后，整批建议按新方向整体替换；断网时给出明确的联网提示。

**为什么：** 选礼物是许多人的痛点；结合对方特点给出具体建议，加上可指定方向，比用户漫无目的地搜索高效得多。

### 14. "去看看"跳转电商搜索
【用户可以】点击礼物建议旁的"去看看"按钮，直接跳转到电商平台的搜索结果页查看对应商品的真实在售情况。
- 默认跳转到淘宝；
- 用户可在设置中选择默认跳转平台，可选项（仅电商类平台）：淘宝、京东、拼多多、唯品会、亚马逊；
- 若用户手机未安装对应 App，则跳转到该平台的网页版；搜索关键词由 AI 生成的礼物名称与收礼对象的特征描述组合而成——有特征描述时格式为"礼物名称 + 特征描述关键词"（如"园艺工具套装 退休 养花"），无特征描述时仅用礼物名称搜索。

**验收标准：** 点击后自动打开所选电商平台（App 或网页），搜索关键词由 AI 生成的礼物名称与收礼对象的特征描述组合而成——有特征描述时格式为"礼物名称 + 特征描述关键词"（如"园艺工具套装 退休 养花"），无特征描述时仅用礼物名称搜索；未安装 App 时能回退到网页。

**为什么：** "有想法"和"能买到"之间还有一步"去哪里找"；把这步自动化，用户从心意到下单的链路才完整。

---

## 五、电子贺卡

### 15. 一键生成电子贺卡
【用户可以】在提醒详情页看到一张已经生成好的电子贺卡。
- 贺卡包含：被提醒对象的称呼或姓名、日子类型（如"生日快乐"/"周年快乐"）、一条由用户选定的祝福语、视觉模板；
- 抬头称呼优先于姓名，但只显示一个；称呼为用户原始输入的关系文本（如"妈妈"），而非归一化后的标准关系名；
- 祝福语由用户从祝福列表中选定（选定哪条，贺卡就采用哪条）；在用户选定祝福语之前，贺卡不显示，并在该位置给出明确的文字引导（提示需先选定一条祝福语才能生成贺卡）；用户选定祝福语后点击"生成贺卡"按钮，贺卡第一次生成；之后更换祝福语或切换模板，需再次点击"重新生成贺卡"按钮才会按新内容重新渲染；"生成贺卡" / "重新生成贺卡"按钮始终显示；
- 模板规则：生日用生日模板；节日用 3 张节日模板（通用节日模板 1 张 + 过年模板 1 张[圣诞/春节] + 情人节模板 1 张[情人节/七夕]）按类型自动匹配；纪念日提供 3 种预设模板（2 种通用 + 1 种结婚/恋爱纪念日模板）供用户选择——结婚/恋爱纪念日模板通过关键词匹配纪念日名称自动推荐（如用户命名含"结婚""婚礼""恋爱"等），匹配不上时默认使用通用模板，用户仍可手动切换到任意模板；纪念日模板（仅纪念日，生日和节日为自动匹配）在贺卡生成后仍可重新切换——每次切换都需手动点击"重新生成贺卡"才会按新模板重新渲染；

- 无网络或贺卡生成失败时，给出明确的提示与重试入口，不清空用户已选定的祝福语。

**验收标准：** 用户选定一条祝福语后，贺卡才显示并使用该条祝福语；抬头只显示称呼或姓名其中一个；纪念日用户可从 3 种模板中选择，生日/节日自动套用对应模板；用户无需编辑即可使用；生成异常时有明确提示且不丢失已选祝福语。

**为什么：** 纯文字祝福在有些关系里显得不够用心，贺卡补上视觉质感；"选定哪条用哪条"保证贺卡文字与用户最终想要的一致。

### 16. 贺卡导出为图片
【用户可以】将生成的电子贺卡导出为图片文件，保存到手机相册或通过任意聊天软件发送。

**验收标准：** 导出后的图片在主流聊天软件（微信、WhatsApp、iMessage 等）中显示正常，文字清晰可读，视觉元素完整无缺失。

**为什么：** 贺卡要能脱离 App 传播才有意义；导出为标准图片格式，用户想怎么发就怎么发。

---

## 六、设置与偏好

### 17. 个人信息与偏好设置
【用户可以】在 App 中设置自己的昵称或称呼方式，该称呼将自动出现在祝福语的落款处。
- 若用户未设置昵称，落款按关系反推：母亲/父亲 → "你的孩子"；妻子 → "你的丈夫"；丈夫 → "你的妻子"；公公/婆婆 → "你的儿媳"；岳父/岳母 → "你的女婿"；
- 儿子/女儿及其他关系（朋友、同事、客户、兄弟姐妹等）无法唯一反推对称身份（儿子/女儿需用户性别才能确定"你的爸爸/你的妈妈"），落款默认留空，并在详情页引导用户补充；
- 用户在提醒详情页可临时修改落款，无需回到设置页。

**验收标准：** 设置昵称后，每条祝福语或贺卡末尾显示用户设定的落款；未设置时按关系反推落款；详情页临时修改的落款对当次生成立即生效。

**为什么：** 祝福语落款如果始终是系统默认名会很尴尬；关系反推 + 详情页可改，兼顾了"少设置"与"可定制"。

---

## 七、历史记录

### 18. 历史记录查看
【用户可以】查看过往生成过的祝福、礼物、贺卡记录。
- 历史记录保存在本地；
- 结构：历史记录顶层为"已过提醒日"的列表（即功能 5 中已过日子的记录），点进某个提醒日后显示该日子的"祝福、礼物、贺卡"内容；
- 历史记录只记录"某年某日发生过某次提醒"这一事实，不标注该提醒是"仅一次"还是"每年一次"；
- 写入时机：写入的是**日子本身**（如生日那天），而非每次推送日；日子次日在 App 启动时扫描写入，写入的为该提醒周期内最终缓存的那批内容（用户最后一次重新生成的结果），作为历史记录的主展示版本；用户在提醒周期内多次重新生成过的所有旧版本一并写入子版本列表，可通过版本切换回看；被手动关闭提醒的日子到期后仍会写入历史记录，但祝福/礼物/贺卡区域为空；

**验收标准：** 历史记录顶层列出每个已过提醒日，点进可查看该提醒日的祝福、礼物、贺卡；同一对象多次重新生成的内容按版本保留，均可查看；被手动关闭提醒的日子写入历史后祝福/礼物/贺卡内容为空，点进该条历史记录时显示一句话："仪式就是使某一天与其他日子不同，使某一时刻与其他时刻不同。"

**为什么：** 用户可能想回看上次给谁送了什么、写了什么祝福，避免重复或遗忘；保留旧版本让"重新生成"没有后悔成本。

---

## 八、首次启动

### 19. 首次启动引导页
【用户可以】在首次启动 App 时进入引导页，了解 App 的核心功能与价值。

**验收标准：** 首次启动时展示引导页；用户完成引导后可进入主界面开始添加联系人；再次启动不再重复展示引导页。

**为什么：** 本产品是"主动推送"模式，用户需要先理解"填人→等提醒"的闭环，引导页能在用户流失前建立预期。

---

## 九、页面与导航结构

### 20. 整体页面结构
【用户可以】通过清晰的导航结构访问 App 的全部功能。

**全局结构：** 底部 3 个 Tab ——「提醒」「联系人」「历史」；设置通过页面右上角入口进入（不占 Tab）；首次启动先进引导页。

```
引导页（仅首次启动）
   ↓
┌──────────────────────────────────┐
│  ○ 提醒      ○ 联系人     ○ 历史  │  ← 底部 3 Tab
└──────────────────────────────────┘
设置入口：各主页面右上角
推送通知点击 ──(深链)──→ 提醒详情页
```

**验收标准：** 底部固定 3 个 Tab；设置可在主页面进入；推送点击可直接打开对应提醒详情页。

**为什么：** 3 个 Tab 覆盖"看提醒 / 管人 / 查往事"三个核心场景，设置属低频操作不占 Tab。

---

### 21. 提醒 Tab（首页）
**页面构成：**
- 提醒列表：未关联联系人的节日与联系人日子**混排在同一列表**，默认按剩余天数升序；支持切换为**按联系人排序**的查看方式——按联系人排序时，每个联系人各自成组，未关联联系人的节日单独归入一个"节日"分组，排在所有联系人分组之后；
- 生效中 / 已过期**分区展示**：生效中的提醒在上，过期的提醒在列表**最下方**单独分区，与生效中的提醒层级分开；
- 列表项展示：联系人称呼（或节日名）+ 日子类型 + 剩余天数 + 日期；同一天的不同条目不会合并（不同节日之间、节日与个人日子之间均不合并），仅同一节日的纯节日提醒与关联联系人提醒才会合并；
- 被局部关闭提醒的条目灰显并带关闭标记；
- 空态（未添加联系人）：引导用户去添加联系人。

**点击列表项** → 提醒详情页。

**验收标准：** 生效中与已过期在视觉上分区清晰、互不混淆；两种排序方式可切换；同一天的不同条目各自独立展示、不合并（仅同一节日的纯节日提醒与关联联系人提醒才合并为一条）；关闭提醒的条目有明确标记。

**为什么：** 一个列表承载全部提醒，避免用户在多个页面间来回找；分区与排序切换兼顾"看全局"和"聚焦某人"两种使用习惯。

---

### 22. 联系人 Tab
- **列表页**：展示全部联系人，每项显示称呼、关系、最近的日子与剩余天数；右上角"＋"新增；
- **详情页**：展示关系、特征描述、日子列表；日子区分"手动设定"与"身份自动关联"（自动的带标签）；每个日子独立提醒开关；提供"＋ 添加纪念日"与"删除联系人"；
- **表单页**：新增 / 编辑联系人，必填校验（姓名、关系、生日），关系输入时实时提示归一化识别结果（如输入"妈咪"提示"识别为：母亲"）。

**验收标准：** 联系人列表与详情信息完整可查；手动日子与自动关联日子可区分；每个日子可独立开关提醒；新增时必填项缺失无法保存。

**为什么：** 联系人是一切提醒的数据源，详情页需要让用户看清"哪些日子是系统的、哪些是我加的"，才能放心管理。

---

### 23. 历史 Tab
- **列表页**：顶层为"已过提醒日"列表（倒序），不标注该提醒是"仅一次"还是"每年一次"；
- **详情页**：点进某个提醒日后，显示该日的祝福、礼物、贺卡；支持切换查看不同重新生成版本；支持在该页面删除该条历史（删除不影响提醒列表）。

**验收标准：** 历史顶层列出每个已过提醒日；点进可查看该日的祝福/礼物/贺卡；版本可切换回看；此页删除不连带删除提醒列表中的条目。

**为什么：** 历史是"档案区"，只记录"某年某日发生过一次提醒"这一事实，保持简单直接。

---

### 24. 设置页
- 我的昵称 / 落款（功能 17）；
- 提醒提前天数（功能 7，范围 1–60）；
- 推送通知总开关（功能 9，优先级最高——关闭后所有推送均不发，含个人日子和通用节日）；
- 通用节日提醒开关（功能 8，含总开关与 2 个子开关，见功能 8）；
- 默认电商平台（功能 14，淘宝 / 京东 / 拼多多 / 唯品会 / 亚马逊）。

- 提醒规则说明页：从设置页的"提醒提前天数"旁可进入，完整展示功能 6 和功能 7 的提醒节奏规则（含"提前天数 < 7 时每天提醒"等特殊规则）；联系人详情页的"添加纪念日"弹窗中也有一个小文字链接可跳至此说明页。

**验收标准：** 上述 5 项设置均可修改且立即生效；提醒规则说明页可从设置页和联系人详情页的"添加纪念日"弹窗两个入口访问。

**为什么：** 所有低频的全局配置集中一处，与高频的内容页面分离。

---

### 25. 提醒详情页（核心页，两个变体）
**变体 A —— 关联联系人的提醒**（个人日子、及关联到联系人的节日）：
- 顶部：被提醒对象 + 日子类型 + 剩余天数 + 日期；
- 祝福区：3 条祝福语 + "重新生成"；进入页面即实时生成，退出再进显示缓存的那批；首次进入时均未选中，等待用户手动选择；点击祝福语文本 = 选定（选中态高亮），每条旁带独立复制 icon；点击"重新生成"后旧批替换，已选中的贺卡文案回到未选中状态，用户需重新选择；
- 礼物区：5 条礼物建议 + "礼物方向"输入框 + 生成按钮；每条带"去看看"按钮；
- 贺卡区：位于祝福列表下方；**未选定祝福语前不显示贺卡，并在该位置给出明确引导**（提示需先选定一条祝福语）；"生成贺卡"/"重新生成贺卡"按钮始终显示——未选定祝福语时点击该按钮弹出提示"请先选定祝福语"；用户选定祝福语后点击"生成贺卡"，贺卡首次生成；之后更换祝福语或切换纪念日模板（仅纪念日），需再次点击"重新生成贺卡"才会重新渲染；若用户点击祝福区的"重新生成"换了一批祝福语，已选中的贺卡文案回到未选中状态，需用户重新选择一条后再生成贺卡；落款可临时修改；导出图片。

**变体 B —— 未关联联系人的节日**：
- 顶部：节日名 + 剩余天数 + 日期；
- 主体：一个"生成节日贺卡"按钮；用户点击后生成一张给用户本人的节日祝福贺卡（文案无需选择）；生成后缓存，退出再进显示缓存中的贺卡；用户可点击"重新生成"获取新的贺卡；节日当天过后，该贺卡存入历史记录；可导出图片。

**验收标准：** 变体 A 进入即呈现祝福与礼物，断网时明确提示联网；礼物区可输入方向并整批重新生成；贺卡在未选定祝福语时以引导文案占位、选定后立即呈现并可导出；变体 B 需用户点击才生成贺卡。

**为什么：** 详情页是用户完成"心意落地"的唯一场所——看祝福、挑礼物、发贺卡都在这一页完成，减少跳转就是减少流失。

---

### 26. 关键跳转关系
```
推送通知 ──────→ 提醒详情页（变体 A 或 B）
提醒 Tab ──点击提醒──→ 提醒详情页
提醒 Tab ──过期分区点击──→ 切换到历史 Tab 并打开对应详情页（底部 Tab 高亮同步变为「历史」，返回键回到历史列表页）
联系人 Tab ──点击──→ 联系人详情 ──编辑──→ 表单页
联系人 Tab ──＋──→ 表单页（新增）
历史 Tab ──点击──→ 历史详情页
设置入口 ──各主页面右上角──→ 设置页
错过提醒弹窗 ──→ 提醒 Tab
```

**验收标准：** 上述跳转均可达且返回路径正确。

**为什么：** 明确跳转关系，避免开发时出现"某个页面进得去出不来"的断头路。

---

## 以后再做

以下功能不在 v1 范围内，仅供后续版本规划参考：

- 自定义添加通用节日之外的个人节日
- 祝福语风格选择（幽默、正式、文艺、简短）
- 更多贺卡模板 / 自定义模板（v1 仅纪念日提供 3 种可选，生日/节日按类型自动匹配）
- 礼物预算范围设置
- 联系人分组管理（家庭/工作/朋友分组）
- 从手机通讯录批量导入联系人
- 农历生日支持（联系人按农历计算生日）
- 数据备份与多设备同步
- 提醒推送的"勿扰时段"设置
- 感恩节等更多国际节日的扩展支持