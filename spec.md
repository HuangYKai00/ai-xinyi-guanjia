# AI 心意管家 — 产品规格文档（v1.1）

## 一、联系人管理

### 1. 添加联系人
【用户可以】将自己在乎的人录入 App，包括家人、挚友、重要客户等。
- 必填项：姓名、关系、生日（生日为必填，纪念日为选填，但至少要有生日）；
- 选填项：特征描述（允许不填，保存时非强制）；
- "关系"为自由文本，由用户自行输入（如"大学室友老王""客户"）；系统会将常见同义表达归一化到标准关系，用户界面仍展示其原始输入，归一化结果仅用于身份自动关联、关系反推落款等内部逻辑（对照表见下方）；
- 一个联系人可添加多个日子（一对多，无数量上限）；
- 每个纪念日用户可自定义命名（如"结婚纪念日""入职纪念日"）；
- 生日可填写出生年份，但非强制。

**关系同义归一化对照表（v1）：**

| 标准关系 | 常见同义表达 |
|---|---|
| 母亲 | 妈妈、母亲、妈咪、老妈、娘、娘亲 |
| 父亲 | 爸爸、父亲、爸、老爸、爹、爹爹 |
| 妻子 | 老婆、妻子、太太、夫人、内人 |
| 丈夫 | 老公、丈夫、先生、夫君 |
| 儿子 | 儿子、小子、男娃 |
| 女儿 | 女儿、闺女、丫头、女娃 |
| 公公 | 公公、家公 |
| 婆婆 | 婆婆、家婆、婆母 |
| 岳父 | 岳父、老丈人、丈人、岳丈 |
| 岳母 | 岳母、丈母娘、岳母娘 |

存在歧义的表达（如"爱人"可指丈夫或妻子、"媳妇"在部分地区指儿媳）不纳入自动归一化，由用户手动明确。

**验收标准：** 成功录入后，联系人的姓名、关系、日子、特征描述等全部保存并可随时查看；必填项缺失时无法保存；同一个联系人名下可以存在并展示多个日子。

**为什么：** 联系人是所有后续心意推送的基础数据；一对多的日子模型符合真实生活——一个人往往有多个值得纪念的日子。

### 2. 日子的来源（手动设定 + 身份自动关联）
【用户可以】通过两种方式获得日子：① 手动为联系人设定纪念日；② 由联系人身份自动关联——例如"妻子"自动关联妇女节，"母亲"自动关联妇女节和母亲节。身份识别基于归一化后的标准关系（见功能 1），系统识别到对应关系时自动为联系人补上相关节日。

**验收标准：** 手动设定的纪念日与身份自动关联的日子在联系人详情中都能被看到和区分；自动关联的日子随关系字段变化而增减；儿子/女儿因无法判断年龄，不自动关联儿童节，用户如需可为子女手动添加儿童节。

**关系 → 自动关联节日映射表：**

| 标准关系 | 自动关联节日 |
|---|---|
| 母亲 | 妇女节、母亲节 |
| 父亲 | 父亲节 |
| 妻子 | 妇女节 |
| 丈夫 | 无 |
| 儿子 | 无（不自动关联儿童节，如需可手动添加） |
| 女儿 | 无（同上） |
| 公公 | 父亲节 |
| 婆婆 | 妇女节、母亲节 |
| 岳父 | 父亲节 |
| 岳母 | 妇女节、母亲节 |

**为什么：** 用户往往只记得填生日，漏掉与身份相关的节日。自动关联能兜住这些高价值的提醒场景。

### 3. 编辑联系人信息
【用户可以】随时修改已录入联系人的任意信息（姓名、关系、日期、特征描述等）。

**验收标准：** 修改后立即生效，后续的祝福生成和礼物推荐均基于最新信息；关系字段变化会触发身份自动关联日子的重新计算。

**为什么：** 人的状态会变化（退休、换工作、新爱好），信息保持准确才能生成贴切的心意。

### 4. 删除联系人
【用户可以】从列表中移除不再需要关心的联系人。

**验收标准：** 删除后，与该联系人相关的所有提醒不再触发；已生成的祝福、礼物、贺卡等历史内容保留在本地，仍可在历史记录中查看；已生成但尚未写入历史记录（日子未到）的祝福在删除时随联系人一并丢失，不做保留。

**为什么：** 人际关系会变化，用户应有完全的控制权；同时历史内容保留，避免误删导致过去的心意记录一并消失。

### 5. 已过日子的处理
【用户可以】看到已经过去的日子。按生效方式不同处理："每年一次"的日子继续保留在待提醒列表中，自动进入下一年度提醒；"仅提醒一次"的日子在生效过后移动到"已过期"列表，不再触发提醒。已过的日子会同步保存进历史记录。

**验收标准：** "每年一次"的日子到期后仍在待提醒列表，自动进入下一年度；"仅一次"的日子到期后移动到已过期列表，提醒开关永久关闭；已过日子的记录保存在历史记录中，历史记录里的条目只能在该页面手动删除，不会因提醒列表（待提醒或已过期）中的删除而被连带删除。

**为什么：** 保留记录是必要的，但过期日子若继续提醒会变成骚扰；待提醒/已过期分离，加上历史记录独立管理，让用户对每个阶段都有清晰掌控。

---

## 二、智能提醒

### 6. 特殊日子自动提醒
【用户可以】收到 App 在联系人生日或纪念日临近时的主动推送。
- 提醒节奏：按距离日子的剩余天数计算——剩余 ≥7 天时每周提醒一次，触发日为与日子当天相同的星期几（如日子是周三，则每周三提醒，第 7 天为每周提醒的最后一次）；剩余 4~6 天时不提醒；剩余 ≤3 天时每天提醒一次（第 3、2、1 天及当天均提醒）；
- 周期规则：生日每年一次；纪念日默认仅生效一次，若需每年重复提醒由用户手动开启"每年一次"；"每年一次"的纪念日到期后自动进入下一年度提醒，"仅一次"的到期后提醒开关永久关闭；
- 局部关闭（两个粒度）：用户可手动关闭单个日子的提醒，也可关闭整个联系人的提醒；
- 同一天有多个提醒时，合并为一条通知（含通用节日提醒与身份自动关联产生的个人提醒叠加）；
- 2 月 29 日生日：非闰年时于 2 月 28 日按生日提醒（提前一天）。

**验收标准：** 通知内容须包含联系人姓名、日子类型（生日/纪念日）以及剩余天数；节奏按"≥7 天每周一次、4~6 天静默、≤3 天每天一次"执行；同一天多个日子合并为一条通知，点开后展示多个入口，再点进可查看单个日子的详细信息。

**为什么：** 用户可能忙碌或遗忘，主动提醒是"AI 主动关怀"的核心价值；由疏到密的提醒节奏，在不过度打扰的前提下保证用户不会错过。

### 7. 提醒提前天数可调整
【用户可以】自行设置希望提前多少天收到提醒，可调范围为 1–60 天；提前天数决定提醒窗口的起点，窗口内按功能 6 的节奏提醒（如设为 30 天，则从距日子 30 天起进入每周提醒，最后 3 天转为每天提醒）；即使只设置提前 1 天，日子当天仍会提醒一次。

**验收标准：** 用户修改后，该规则对个人日子与通用节日共同生效（统一一套设置），下一次提醒按新设置触发；超出 1–60 范围的值不允许保存。

**为什么：** 不同用户的准备习惯不同——有人提前一周准备礼物，有人前一天才想起来；统一一套设置降低理解成本。

### 8. 通用节日自动提醒
【用户可以】在通用节日临近时收到提醒。
- 提醒对象默认是"用户自己"，不绑定具体联系人；
- 仅当节日明确指向特定人群、且用户已设置了相关关系时，才自动关联对应联系人（例如母亲节自动关联"关系=母亲"的联系人）；关联联系人后，该节日详情页与个人日子提醒一致，为该联系人生成祝福、礼物、贺卡；同一节日触发的"纯节日提醒"与"关联联系人提醒"若落在同一天，按功能 6 的"同日合并"规则合并为一条通知；
- 未关联到联系人的节日（用户未添加任何联系人，或节日本身不指向特定人群）仍照常推送提醒；点开详情页后，用户需点击一次"生成"按钮，才会生成一张给用户本人的节日祝福贺卡，文案无需用户选择；
- 通用节日提醒默认开启，可在设置中关闭；
- 节日日期以中国通用标准为准，中国没有的节日以国际通用标准为准；农历日期通过读取手机日期获得。

**v1 支持节日清单：**

中国法定节假日：元旦、春节、清明节、劳动节、端午节、中秋节、国庆节

传统节日：元宵节、七夕节、重阳节、除夕

特定人群/主题节日：母亲节、父亲节、妇女节、儿童节

西方/商业节日：圣诞节、情人节

**验收标准：** 每个已支持的节日到来前，App 按提前天数设置发出推送，通知包含节日名称与剩余天数；关联规则正确触发（母亲节→母亲）；清单之外的节日不推送。

**为什么：** 通用节日是表达心意的高频场景；"提醒自己 + 条件关联联系人"的模型，既能覆盖纯节日提醒，也能在关系匹配时升级为个性化建议。

### 9. 推送通知开关
【用户可以】选择开启或关闭整个 App 的推送通知功能。

**验收标准：**
- 关闭后，所有提醒（个人日子 + 通用节日）均不再推送，重新开启后恢复；
- 关闭期间错过的提醒不补发，但恢复后弹窗告知用户关闭期间错过了谁的提醒；
- 系统通知权限被关闭时，恢复权限后显示"错过提醒"弹窗。

**为什么：** 强制推送会造成反感甚至卸载；权限被系统关闭是常见情况，主动告知错过的日子体现"体贴"，避免用户不知情地错过重要日子。

---

## 三、祝福内容生成

### 10. 个性化祝福生成
【用户可以】从推送点进详情页时，实时看到已经拟好、结合被提醒对象特征量身定制的祝福语。
- 祝福语在用户点进详情页时实时生成；生成后缓存，用户退出再进详情页仍显示同一批，直到主动点击"重新生成"才更新；
- 生成依据：关系类型、特征描述、日子类型（生日/纪念日/节日）；
- 无特征描述时，按关系类型生成，并引导（非强制）用户补充特征描述；
- 无网络时提示用户连接网络；
- 每条祝福语长度不超过 600 字，纯文本格式，适合直接复制到聊天软件发送。

**验收标准：** 祝福语与被提醒对象直接相关（如结合"爱养花，今年刚退休"），而非泛泛通用祝福；点进详情页即能呈现；断网时给出明确的联网提示而非空白或错误。

**为什么：** 用户最头疼的不是"要不要送祝福"，而是"不知道该说什么"；直接给出结合对方特点的祝福语，用户只需挑选或微调即可发送。

### 11. 祝福重新生成
【用户可以】对生成的祝福不满意时，一键重新生成全部祝福语。
- 默认每次生成 3 条，不支持调整条数；
- 重新生成为全量重新生成，不支持单条单独重新生成。

**验收标准：** 每次点"重新生成"后，3 条祝福语整体被替换为新的一批；条数始终为 3。

**为什么：** 固定条数保持界面简洁、成本可控；"全部重来"比逐条重试更符合用户"换一批"的心智模型。

### 12. 祝福语一键复制
【用户可以】点击任意一条祝福语将其完整复制到剪贴板，方便粘贴到任意聊天软件中。

**验收标准：** 点击后，系统剪贴板中已包含该祝福语的完整文本，可立即在微信、短信等应用中粘贴使用。

**为什么：** 祝福语的最终目的是"发出去"；从看到到发出中间如果还要手动输入一遍，就等于没做。

---

## 四、礼物推荐

### 13. 个性化礼物建议
【用户可以】在提醒详情页看到多条与被提醒对象匹配的礼物建议。
- 生成时机：与祝福语一致，点进详情页时实时生成；
- 默认每次推荐 5 个；
- 生成依据：特征描述、日子类型、关系类型；
- 支持用户输入"礼物方向"（如"实用""浪漫""预算500以内"）后重新生成；
- 重新生成为全量重新生成，不支持单条单独重新生成。

**验收标准：** 礼物建议结合对方特点（如"爱养花"建议园艺工具、"刚退休"建议旅行用品），而非随机推荐；输入礼物方向后，整批建议按新方向整体替换。

**为什么：** 选礼物是许多人的痛点；结合对方特点给出具体建议，加上可指定方向，比用户漫无目的地搜索高效得多。

### 14. "去看看"跳转电商搜索
【用户可以】点击礼物建议旁的"去看看"按钮，直接跳转到电商平台的搜索结果页查看对应商品的真实在售情况。
- 默认跳转到淘宝；
- 用户可在设置中选择默认跳转平台，可选项（仅电商类平台）：淘宝、京东、拼多多、唯品会、亚马逊；
- 若用户手机未安装对应 App，则跳转到该平台的网页版。

**验收标准：** 点击后自动打开所选电商平台（App 或网页），并已填入与该礼物相关的搜索关键词，用户可直接浏览和购买；未安装 App 时能回退到网页。

**为什么：** "有想法"和"能买到"之间还有一步"去哪里找"；把这步自动化，用户从心意到下单的链路才完整。

---

## 五、电子贺卡

### 15. 一键生成电子贺卡
【用户可以】在提醒详情页看到一张已经生成好的电子贺卡。
- 贺卡包含：被提醒对象的称呼或姓名、日子类型（如"生日快乐"/"周年快乐"）、一条由用户选定的祝福语、视觉模板；
- 抬头称呼优先于姓名，但只显示一个；称呼为用户原始输入的关系文本（如"妈妈"），而非归一化后的标准关系名；
- 祝福语由用户从祝福列表中选定（选定哪条，贺卡就采用哪条）；在用户选定祝福语之前，贺卡不显示；
- 模板规则：生日用生日模板；节日用 3 张节日模板（通用节日模板 1 张 + 过年模板 1 张[圣诞/春节] + 情人节模板 1 张[情人节/七夕]）按类型自动匹配；纪念日提供 3 种预设模板（2 种通用 + 1 种结婚/恋爱纪念日模板）供用户选择。

**验收标准：** 用户选定一条祝福语后，贺卡才显示并使用该条祝福语；抬头只显示称呼或姓名其中一个；纪念日用户可从 3 种模板中选择，生日/节日自动套用对应模板；用户无需编辑即可使用。

**为什么：** 纯文字祝福在有些关系里显得不够用心，贺卡补上视觉质感；"选定哪条用哪条"保证贺卡文字与用户最终想要的一致。

### 16. 贺卡导出为图片
【用户可以】将生成的电子贺卡导出为图片文件，保存到手机相册或通过任意聊天软件发送。

**验收标准：** 导出后的图片在主流聊天软件（微信、WhatsApp、iMessage 等）中显示正常，文字清晰可读，视觉元素完整无缺失。

**为什么：** 贺卡要能脱离 App 传播才有意义；导出为标准图片格式，用户想怎么发就怎么发。

---

## 六、设置与偏好

### 17. 个人信息与偏好设置
【用户可以】在 App 中设置自己的昵称或称呼方式，该称呼将自动出现在祝福语的落款处。
- 若用户未设置昵称，落款按关系反推：母亲/父亲 → "你的孩子"；妻子 → "你的丈夫"；丈夫 → "你的妻子"；公公/婆婆 → "你的儿媳"；岳父/岳母 → "你的女婿"；
- 儿子/女儿及其他关系（朋友、同事、客户、兄弟姐妹等）无法唯一反推对称身份（儿子/女儿需用户性别才能确定"你的爸爸/你的妈妈"），落款默认留空，并在详情页引导用户补充；
- 用户在提醒详情页可临时修改落款，无需回到设置页。

**验收标准：** 设置昵称后，每条祝福语或贺卡末尾显示用户设定的落款；未设置时按关系反推落款；详情页临时修改的落款对当次生成立即生效。

**为什么：** 祝福语落款如果始终是系统默认名会很尴尬；关系反推 + 详情页可改，兼顾了"少设置"与"可定制"。

---

## 七、历史记录

### 18. 历史记录查看
【用户可以】查看过往生成过的祝福、礼物、贺卡记录。
- 历史记录保存在本地；
- 结构：历史记录顶层为"已过提醒日"的列表（即功能 5 中已过日子的记录），点进某个提醒日后显示该日子的"祝福、礼物、贺卡"内容；
- 写入时机：提醒过后（日子到来之后）才会写入历史记录；
- 每次重新生成会保留旧版本，用户可回看不同批次的内容。

**验收标准：** 历史记录顶层列出每个已过提醒日，点进可查看该提醒日的祝福、礼物、贺卡；同一对象多次重新生成的内容按版本保留，均可查看。

**为什么：** 用户可能想回看上次给谁送了什么、写了什么祝福，避免重复或遗忘；保留旧版本让"重新生成"没有后悔成本。

---

## 八、首次启动

### 19. 首次启动引导页
【用户可以】在首次启动 App 时进入引导页，了解 App 的核心功能与价值。

**验收标准：** 首次启动时展示引导页；用户完成引导后可进入主界面开始添加联系人；再次启动不再重复展示引导页。

**为什么：** 本产品是"主动推送"模式，用户需要先理解"填人→等提醒"的闭环，引导页能在用户流失前建立预期。

---

## 以后再做

以下功能不在 v1 范围内，仅供后续版本规划参考：

- 自定义添加通用节日之外的个人节日
- 祝福语风格选择（幽默、正式、文艺、简短）
- 更多贺卡模板 / 自定义模板（v1 仅纪念日提供 3 种可选，生日/节日按类型自动匹配）
- 礼物预算范围设置
- 联系人分组管理（家庭/工作/朋友分组）
- 从手机通讯录批量导入联系人
- 农历生日支持（联系人按农历计算生日）
- 数据备份与多设备同步
- 提醒推送的"勿扰时段"设置
- 感恩节等更多国际节日的扩展支持