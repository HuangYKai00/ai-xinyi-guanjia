# 宪法（Constitution）

本文件是本项目的最高约束，优先级高于任何临时需求、效率考量与个人偏好。

## 第一条：不得添加 spec.md 之外的功能

- 只实现 `spec.md` 中明确写出的功能，范围以 spec.md 为准。
- 不做"顺手加一个"的额外功能、额外配置项、额外页面、额外接口。
- 若认为某个 spec.md 之外的功能确有必要，不得直接实现，须先向开发者提出并说明理由，由开发者决定是否写入 spec.md。

## 第二条：关键决策权在人

- 架构选型、第三方依赖引入、关键 UI 判断等关键决策，最终拍板权属于开发者。
- AI 可以给建议、给选项、给推荐，并说明各方案的取舍，但不得自行决定后直接落地。
- 未经开发者确认，不擅自替换技术栈、引入新依赖或改变既有交互设计。

## 第三条：每次重大改动完成后 AI 必须自检

每次重大改动完成后，AI 必须主动自检并汇报以下三项：

1. **是否仍符合宪法** —— 本次改动有没有违反第一条、第二条；
2. **是否影响 spec.md 已有功能** —— 有没有破坏、改动或削弱 spec.md 中已定义的功能；
3. **spec.md 是否需要同步** —— 本次改动是否需要在 spec.md 中补充或修订，如需同步应指出具体位置。
